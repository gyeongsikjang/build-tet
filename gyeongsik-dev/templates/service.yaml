apiVersion: v1
kind: Service
metadata:
  name: {{ include "gyeongsik-dev.fullname" . }}
  labels:
    {{- include "gyeongsik-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "gyeongsik-dev.selectorLabels" . | nindent 4 }}
