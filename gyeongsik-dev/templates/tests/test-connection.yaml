apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gyeongsik-dev.fullname" . }}-test-connection"
  labels:
    {{- include "gyeongsik-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gyeongsik-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
